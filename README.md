# Interview AI Backend

An Express backend for an AI interview application.

## Run locally

```bash
npm install
npm run dev
```

## Production start

```bash
node server.js
```

## Notes

- Create a `.env` file with the required keys before starting the server.
- Install dependencies with `npm install` if `node_modules` is not present.
